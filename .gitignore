# Build output
/build/
/out/
/cmake-build-*/

# FetchContent / CMake cache
/_deps/
**/_deps/
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json
cmake_install.cmake

# IDE
/.vs/
/.vscode/
/.idea/
/.zed/

# LSP / language server caches (clangd's index, etc.)
/.cache/

# Per-user Claude Code overrides — keep out of version control.
/.claude/settings.local.json

# Mirrored from build/<preset>/ by the root CMakeLists for editor LSPs.
/compile_commands.json
*.user
*.suo
*.sdf
*.opensdf
*.vcxproj.user

# OS
Thumbs.db
.DS_Store

# Compiled binaries
*.exe
*.dll
*.so
*.so.*
*.dylib
*.a
*.lib
*.obj
*.o
*.pdb
*.ilk
.scratch/

# ---------------------------------------------------------------------------
# Paid / licensed art assets — local-only, never committed.
# Each entry here is one pack the developer has licensed for use in builds.
# The runtime + asset-mirror pipeline will use them locally, but they MUST
# NOT end up in the public repo.
# ---------------------------------------------------------------------------
/games/my_rpg/assets/timefantasy_characters/
/games/my_rpg/assets/TimeFantasy_TILES_6.24.17/
/games/my_rpg/assets/28 High Quality 16-bit RPG Music/
